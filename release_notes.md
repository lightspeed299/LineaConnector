## v6.3.0 Eval Path & Book PV Extension

- **Add: 評価関数の指定** — 設定に「評価関数」参照行を追加。選択したファイルに合わせてエンジンの `EvalDir`（親フォルダ）/ `EvalFile` / `DNN_Model` を自動設定する（エンジンが対応オプションを宣言していない場合はスキップしてログ表示）。未設定ならエンジン既定のまま
- **Add: 定跡PVの自動延長** — 定跡の候補手ごとに、指した先の局面を再び定跡で引いて先頭手を連鎖し、**最大10手の読み筋**として表示（従来は .db の応手含め最大2手・.ybb は1手のみだった）。循環は自動打ち切り
- 情報パネルの評価関数表示行は設定欄へ移動（自動検出表示は廃止し、明示指定に一本化）

## v6.2.0 YBB Format & FlippedBook

- **Add: やねうら王バイナリ定跡DB (`.ybb`) 対応**
  - 新ペタショック定跡など `.ybb` 形式の定跡ファイルをそのまま選択可能に
  - 局面表現(PackedSfen 256bit)を完全移植し、本家生成バイナリとの互換を検証済み
  - 大容量ファイル(32MB超)はメモリに載せず44バイト固定長レコードの二分探索で検索（数百MB級でも常駐メモリほぼゼロ）
- **Add: FlippedBook（盤面180°反転ヒット・常時有効）**
  - 定跡に登録された局面の先後反転形でもヒットし、指し手を反転して表示
  - やねうら王のエンジンオプション `FlippedBook` オン相当
- 定跡テストを41本に拡充（ShogiHome実生成の.ybbバイナリとの交差検証を含む）

## v6.1.1 Opening Book Support

- **Add: 定跡ファイル対応（.db 形式）**
  - 設定画面で定跡ファイルを選択すると、Web研究室の「定跡を利用」トグルで定跡モードを切替可能に
  - 定跡にある局面では、候補手が評価値・採用数つきで通常の候補手行として即座に表示される（エンジンを回さないためCPU/メモリを消費しない）
  - 手を進めて定跡を外れると自動的にエンジン解析へ切り替わる
  - 大容量定跡（32MB超）はメモリに載せずファイル上で検索する省メモリ設計
  - 局面の手数に依存せずヒットする
- **Add: 評価関数の表示** — メイン画面に検出された評価関数ファイル名を常時表示（どの評価関数で動いているかを確認できる）
- **設定同期の拡張**: 定跡の利用状態・ファイル名・読み込み状況をWeb側と同期
- v6.1.0 からの変更: 表記調整・評価関数行の追加（v6.1.0 は本リリースに置き換え）

## v6.1.0 Opening Book Support

- **Add: 定跡ファイル対応（やねうら王 .db 形式）**
  - 設定画面で定跡ファイルを選択すると、Web研究室の「定跡を利用」トグルで定跡モードを切替可能に
  - 定跡にある局面では、候補手が評価値・採用数つきで通常の解析行として表示される（エンジンは回さないためCPU/メモリを消費しない）
  - 定跡を外れた局面では自動的にエンジン解析へ切り替わる
  - 大容量定跡（32MB超）はメモリに載せずファイル上で検索する省メモリ設計
- **設定同期の拡張**: 定跡の利用状態・ファイル名・読み込み状況をWeb側と同期

## v6.0.0 USI Engine Overhaul

- **Rewrite: USIエンジン制御を状態機械ベースへ全面刷新**
  - stdout を行単位で処理し、高NPS時に評価値・読み筋が欠落する可能性を根絶
  - `stop` 後は `bestmove` を待ってから次の解析を開始する USI 準拠の直列化
  - エンジンの option 宣言を解析し、対応オプションだけを型検証・範囲制限つきで送信
  - `usiok` / `readyok` の待ち合わせとタイムアウト（応答不能エンジンは安全に破棄・自動再起動）
  - `score mate +`（手数未確定の詰み）の正しい解釈
- **Add: USI通信ログ** — エンジンとの生のやりとり直近100件を「⚙ USI通信」ボタンでコピー可能に（トラブル報告用）
- **Add: 解析結果の構造化送信** — 評価値・深さ・NPS・読み筋を解析済みデータとして送信（Web側の表示品質向上。旧形式も併送するため古い環境でも動作）
- **Fix: FV_SCALE 設定キーの表記を統一**（旧 `fv_scale` は自動移行）
- **Add: .bat / .cmd ラッパー経由のエンジン起動に対応**
- **内部: モックエンジンによる19本のユニットテスト+実Electron E2Eテストを整備**

## v5.1.2 Icon Background Fix

- **Fix: Linea Connector のアプリアイコン背景を修正**
  - 現行 Linea の青いロゴを黒背景つきで切り出し直し
  - Windows のアプリ本体、タスクバー、デスクトップ/スタートメニューショートカット、インストーラー/アンインストーラー用 ICO を再生成
  - 透明背景になっていた v5.1.1 のアイコンを、Linea 本体のアプリアイコンと同じ黒背景系の見た目へ修正

## v5.1.1 Icon Refresh

- **Update: Linea Connector のアプリアイコンを更新**
  - Windows のアプリ本体、タスクバー、デスクトップ/スタートメニューショートカット、インストーラー/アンインストーラーで使うアイコンを現行 Linea の青いロゴへ更新
  - 小さい Windows アイコンサイズでも読みやすいよう、ワードマークを外したロゴ本体の ICO を同梱

## v5.1.0 Automatic Idle Updates

- **Update: 自動ダウンロードとアイドル時の自動適用**
  - 更新が見つかった場合は自動でダウンロード
  - ダウンロード完了後、30分間リクエストや設定変更がなければ自動で再起動して適用
  - 解析中またはエンジン設定変更中は自動適用を延期
  - すぐ適用したい場合の「今すぐ再起動」操作は維持

## v5.0.0 Linea Connector

- **Release: Linea Connector として再リリース**
  - アプリ表示版、更新メタデータ、GitHub release を `v5.0.0` に更新
  - 新規セットアップ時の接続先は `https://api.lineashogi.com`
  - 自動アップデート先は `lightspeed299/LineaConnector`
- **Migration: 既存設定の自動移行を維持**
  - 保存済みの旧 API URL は起動時に Linea API へ更新
  - 旧設定フォルダが残っている場合は、現在の設定フォルダへコピーして整理
  - Windows 上の同一アプリ更新を保つため、内部 appId は legacy 値を維持
